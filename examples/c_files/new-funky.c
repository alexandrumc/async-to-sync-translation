const int FIRST_ROUND = 1;
const int LAST_ROUND = 4;
const int SECOND_ROUND = 2;

typedef struct Mbox
{
  int messages[100];
  int num_msg;
} Mbox;

int main()
{
  Mbox mbox;
  mbox.num_msg = 0;
  while (1)
  {
    lab = FIRST_ROUND;
    sendto(broadcast);
    if (mbox.num_msg > (n / 2))
    {
      if (smth)
      {
        lab = CEVA;
        i++;
        if (other)
        {
          m++;
          if (dama)
          {
            if (andrei)
            {
              x++;
              lab = NEW_ONE;
            }

            p++;
          }

          o++;
        }
        else
        {
          aa++;
        }

        lab = ALTCEVA;
      }
      else
      {
        k--;
      }

      if (mai_mare)
      {
        if (ceva)
        {
          j++;
          if (c)
          {
            bb++;
            lab = LAST_ROUND;
          }

          if (undrum)
          {
            drum++;
            if (smecherie)
            {
              nn++;
            }
            else
            {
              plm++;
            }

            nush_sef--;
          }
          else
          {
            se_poate += 10;
          }

          lasa--;
          bine++;
        }

        lab = SECOND_ROUND;
      }

      i_dont_know = why;
    }

  }

}